{
  "name": "@veridia/shared-types",
  "version": "5.3.0",
  "description": "Shared TypeScript types generated from Drizzle schema",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "build": "echo 'No build needed - types are consumed directly'",
    "clean": "rm -rf src/*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
